#!/usr/bin/env zsh

# ##### Enable Gnome extensions #####
gnome_extensions=(
  "appindicatorsupport@rgcjonas.gmail.com"
  "gTile@vibou"
)
for gnome_extension_id in ${gnome_extensions[@]}; do
  if command -v gnome-extensions >/dev/null 2>&1; then
    gnome-extensions enable $gnome_extension_id
  fi
done
